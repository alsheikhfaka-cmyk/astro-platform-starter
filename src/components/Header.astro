---
const currentPath = Astro.url.pathname;

const navLinks = [
    { href: '/', label: 'الرئيسية' },
    { href: '/services', label: 'خدماتنا' },
    { href: '/about', label: 'من نحن' },
    { href: '/contact', label: 'تواصل معنا' },
];
---

<header class="fixed top-0 right-0 left-0 z-50 bg-white/95 backdrop-blur-sm shadow-sm">
    <div class="container">
        <nav class="flex items-center justify-between h-16 sm:h-20">
            <a href="/" class="flex items-center gap-2 no-underline">
                <span class="text-2xl sm:text-3xl">🔧</span>
                <div>
                    <span class="block text-base sm:text-lg font-bold" style="color: var(--color-primary);">فني مطابخ الرياض</span>
                    <span class="hidden sm:block text-xs" style="color: var(--color-text-light);">خبرة واحترافية</span>
                </div>
            </a>

            <div class="hidden md:flex items-center gap-6">
                {navLinks.map(link => (
                    <a
                        href={link.href}
                        class:list={[
                            'font-semibold text-sm transition-colors no-underline hover:opacity-80',
                            currentPath === link.href ? 'text-accent' : ''
                        ]}
                        style={currentPath === link.href ? 'color: var(--color-accent);' : 'color: var(--color-text);'}
                    >
                        {link.label}
                    </a>
                ))}
                <a href="tel:0591547538" class="btn btn-primary text-sm" style="padding: 0.5rem 1.25rem;">
                    📞 اتصل الآن
                </a>
            </div>

            <button id="mobile-menu-btn" class="md:hidden p-2 cursor-pointer bg-transparent border-none" aria-label="القائمة">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                </svg>
            </button>
        </nav>
    </div>

    <div id="mobile-menu" class="hidden md:hidden bg-white border-t">
        <div class="container py-4 flex flex-col gap-3">
            {navLinks.map(link => (
                <a
                    href={link.href}
                    class="font-semibold py-2 no-underline"
                    style={currentPath === link.href ? 'color: var(--color-accent);' : 'color: var(--color-text);'}
                >
                    {link.label}
                </a>
            ))}
            <a href="tel:0591547538" class="btn btn-primary text-sm mt-2" style="padding: 0.5rem 1.25rem;">
                📞 اتصل الآن
            </a>
        </div>
    </div>
</header>

<script>
    const btn = document.getElementById('mobile-menu-btn');
    const menu = document.getElementById('mobile-menu');
    btn?.addEventListener('click', () => {
        menu?.classList.toggle('hidden');
    });
</script>
